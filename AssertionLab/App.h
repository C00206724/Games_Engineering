#pragma once
#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <cctype>
class App
{
public:
	App();

	bool sizeRange(std::vector<int> & v);
	bool duplicates(std::vector<int>  & v);

private:
	
};

